[project]
name = "tctravel-temporal-compiler"
version = "0.2.0"
description = "Strict itinerary contracts and deterministic temporal dependency graphs"
requires-python = ">=3.11"
dependencies = []

[tool.pyright]
pythonVersion = "3.11"
typeCheckingMode = "strict"
